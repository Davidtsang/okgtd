<div class='stuff-form'>
  <%= form_for(@stuff) do |f| %>

      <%= f.label :content %>
      <br/>
      <%= f.text_field :content %>
      <br/>
      <%= f.hidden_field :parent_id , :value =>@stuff.parent_id %>

      <%= f.submit "Create" %>
  <% end %>
</div>